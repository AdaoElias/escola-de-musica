{
  "name": "escola-de-musica",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de gestão de escola de música - HTML/CSS/JS + Netlify Functions + Supabase",
  "scripts": {
    "dev": "netlify dev",
    "build": "netlify build",
    "deploy": "netlify deploy --prod"
  },
  "devDependencies": {
    "netlify-cli": "^19.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}